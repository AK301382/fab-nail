# 🚀 راهنمای اجرای پروژه در محیط لوکال (Local)

## 📋 پیش‌نیازها

قبل از شروع، مطمئن شوید که موارد زیر روی سیستم شما نصب هستند:

### نرم‌افزارهای مورد نیاز:
- **Node.js** (نسخه 18 یا بالاتر) - [دانلود از اینجا](https://nodejs.org/)
- **Python** (نسخه 3.9 یا بالاتر) - [دانلود از اینجا](https://www.python.org/)
- **MongoDB** (نسخه 5.0 یا بالاتر) - [دانلود از اینجا](https://www.mongodb.com/try/download/community)
- **Yarn** (Package Manager) - نصب با دستور: `npm install -g yarn`
- **pip** (Python Package Manager) - معمولاً همراه Python نصب می‌شود

### بررسی نصب:
برای اطمینان از نصب صحیح، دستورات زیر را در Terminal/CMD اجرا کنید:

```bash
node --version    # باید نسخه 18+ را نشان دهد
python --version  # باید نسخه 3.9+ را نشان دهد
mongo --version   # یا mongod --version
yarn --version
pip --version
```

---

## 📥 مرحله 1: دانلود و آماده‌سازی پروژه

### 1.1 کلون کردن پروژه (اگر از Git استفاده می‌کنید):
```bash
git clone <repository-url>
cd <project-folder>
```

### 1.2 یا اگر فایل zip دارید:
- فایل zip را Extract کنید
- وارد پوشه پروژه شوید

---

## 🗄️ مرحله 2: راه‌اندازی MongoDB

### 2.1 راه‌اندازی MongoDB روی Windows:
```bash
# روش 1: اجرا به صورت Service (توصیه می‌شود)
# MongoDB به صورت خودکار در پس‌زمینه اجرا می‌شود

# روش 2: اجرا دستی
cd "C:\Program Files\MongoDB\Server\<version>\bin"
mongod --dbpath="C:\data\db"
```

### 2.2 راه‌اندازی MongoDB روی macOS:
```bash
# روش 1: با Homebrew
brew services start mongodb-community

# روش 2: اجرا دستی
mongod --config /usr/local/etc/mongod.conf
```

### 2.3 راه‌اندازی MongoDB روی Linux:
```bash
# Ubuntu/Debian
sudo systemctl start mongod
sudo systemctl enable mongod  # برای اجرای خودکار

# بررسی وضعیت
sudo systemctl status mongod
```

### 2.4 تست اتصال MongoDB:
```bash
# وارد MongoDB Shell شوید
mongo
# یا
mongosh

# باید به Mongo متصل شوید و پیام خوش‌آمدگویی ببینید
```

---

## ⚙️ مرحله 3: تنظیمات Backend

### 3.1 وارد پوشه Backend شوید:
```bash
cd backend
```

### 3.2 ایجاد فایل Environment Variables:
فایلی به نام `.env` در پوشه `backend` بسازید:

```bash
# Windows
type nul > .env

# macOS/Linux
touch .env
```

### 3.3 محتوای فایل `.env` را به شکل زیر تنظیم کنید:
```env
# اتصال به MongoDB
MONGO_URL=mongodb://localhost:27017

# نام دیتابیس
DB_NAME=fabulous_nails

# تنظیمات CORS (برای دسترسی Frontend)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
```

### 3.4 نصب کتابخانه‌های Python:
```bash
# Windows
pip install -r requirements.txt

# macOS/Linux (در صورت نیاز به sudo)
pip3 install -r requirements.txt
# یا
sudo pip3 install -r requirements.txt
```

**نکته مهم:** اگر با خطای Permission مواجه شدید:
```bash
pip install --user -r requirements.txt
```

### 3.5 اجرای Backend:
```bash
# روش 1: با Uvicorn (توصیه می‌شود)
uvicorn server:app --host 0.0.0.0 --port 8001 --reload

# روش 2: با Python مستقیم
python -m uvicorn server:app --host 0.0.0.0 --port 8001 --reload
```

**Backend باید روی `http://localhost:8001` اجرا شود**

### 3.6 تست Backend:
یک Terminal/CMD جدید باز کنید و دستور زیر را اجرا کنید:
```bash
curl http://localhost:8001/api/services
# یا مرورگر را باز کنید و به آدرس بالا بروید
```

اگر لیستی (حتی خالی) `[]` دریافت کردید، یعنی Backend با موفقیت اجرا شده! ✅

---

## 🎨 مرحله 4: تنظیمات Frontend

### 4.1 یک Terminal/CMD جدید باز کنید و وارد پوشه Frontend شوید:
```bash
cd frontend
```

### 4.2 ایجاد فایل Environment Variables:
```bash
# Windows
type nul > .env

# macOS/Linux
touch .env
```

### 4.3 محتوای فایل `.env` را به شکل زیر تنظیم کنید:
```env
# آدرس Backend
VITE_BACKEND_URL=http://localhost:8001
```

### 4.4 نصب کتابخانه‌های Node.js:
```bash
yarn install
```

**توجه:** این مرحله ممکن است 2-5 دقیقه طول بکشد.

### 4.5 اجرای Frontend:
```bash
yarn start
# یا
yarn dev
```

**Frontend باید روی `http://localhost:3000` اجرا شود**

مرورگر شما به صورت خودکار باز می‌شود. اگر نشد، آدرس `http://localhost:3000` را در مرورگر وارد کنید.

---

## ✅ مرحله 5: تست و بررسی نهایی

### 5.1 بررسی صفحه اصلی:
- مرورگر را باز کنید: `http://localhost:3000`
- باید صفحه اصلی سایت نمایش داده شود

### 5.2 ورود به پنل ادمین:
```
آدرس: http://localhost:3000/admin/login

نام کاربری: admin
رمز عبور: admin123
```

⚠️ **هشدار امنیتی:** حتماً رمز عبور پیش‌فرض را در محیط Production تغییر دهید!

### 5.3 تست عملکرد:
1. وارد پنل ادمین شوید
2. به بخش "خدمات" بروید
3. یک سرویس جدید اضافه کنید
4. لودینگ و ذخیره‌سازی را بررسی کنید
5. به صفحه اصلی برگردید و سرویس جدید را مشاهده کنید

---

## 🐛 عیب‌یابی مشکلات رایج

### مشکل 1: Backend اجرا نمی‌شود

**خطا: `ModuleNotFoundError`**
```bash
# حل:
pip install -r requirements.txt --force-reinstall
```

**خطا: `Port 8001 is already in use`**
```bash
# Windows - کشتن پروسه روی پورت 8001
netstat -ano | findstr :8001
taskkill /PID <PID> /F

# macOS/Linux
lsof -ti:8001 | xargs kill -9
```

**خطا: `Connection to MongoDB failed`**
- مطمئن شوید MongoDB در حال اجرا است
- آدرس MongoDB در `.env` را بررسی کنید
- فایروال را بررسی کنید

```bash
# تست اتصال MongoDB
mongo --eval "db.version()"
# یا
mongosh --eval "db.version()"
```

---

### مشکل 2: Frontend اجرا نمی‌شود

**خطا: `Command 'vite' not found`**
```bash
# حل:
cd frontend
rm -rf node_modules yarn.lock
yarn install
```

**خطا: `Port 3000 is already in use`**
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# macOS/Linux
lsof -ti:3000 | xargs kill -9

# یا پورت را تغییر دهید
vite --port 3001
```

**خطا: `Failed to fetch from backend`**
- مطمئن شوید Backend در حال اجرا است (`http://localhost:8001`)
- فایل `.env` در پوشه `frontend` را بررسی کنید
- مرورگر را Refresh کنید (Ctrl + F5)

---

### مشکل 3: داده‌ها نمایش داده نمی‌شوند

**راه حل 1: بررسی اتصال به دیتابیس**
```bash
# وارد MongoDB Shell شوید
mongo
# یا
mongosh

# انتخاب دیتابیس
use fabulous_nails

# بررسی Collections
show collections

# بررسی داده‌ها
db.services.find().pretty()
```

**راه حل 2: Seed کردن دیتابیس (اضافه کردن داده‌های نمونه)**
```bash
cd backend

# اجرای اسکریپت Seed
python seed_data.py
```

**راه حل 3: پاک کردن Cache مرورگر**
- در Chrome: `Ctrl + Shift + Delete` (Windows) یا `Cmd + Shift + Delete` (Mac)
- "Cached images and files" را انتخاب کنید
- Clear data کنید

---

## 📂 ساختار پروژه در حال اجرا

```
شما باید 3 Terminal/CMD باز داشته باشید:

┌─────────────────────────────────┐
│ Terminal 1: MongoDB             │
│ → mongod (در پس‌زمینه)          │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ Terminal 2: Backend             │
│ → cd backend                    │
│ → uvicorn server:app --reload   │
│ → Running on: localhost:8001    │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ Terminal 3: Frontend            │
│ → cd frontend                   │
│ → yarn start                    │
│ → Running on: localhost:3000    │
└─────────────────────────────────┘
```

---

## 🎯 نکات مهم

### توصیه‌های امنیتی:
1. ⚠️ **هرگز** فایل‌های `.env` را در Git قرار ندهید
2. ⚠️ رمز عبور پیش‌فرض ادمین را حتماً تغییر دهید
3. ⚠️ در محیط Production از MongoDB Atlas استفاده کنید

### توصیه‌های عملکردی:
1. ✅ برای توسعه، از `--reload` استفاده کنید (تغییرات به صورت خودکار اعمال می‌شود)
2. ✅ از یک Virtual Environment برای Python استفاده کنید:
```bash
# ایجاد Virtual Environment
python -m venv venv

# فعال‌سازی
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# نصب Dependencies
pip install -r requirements.txt
```
3. ✅ لاگ‌های Backend و Frontend را برای عیب‌یابی چک کنید

---

## 🔄 متوقف کردن پروژه

### برای توقف سرویس‌ها:
1. در هر Terminal که Backend یا Frontend اجرا است: `Ctrl + C`
2. برای توقف MongoDB:
```bash
# Windows
net stop MongoDB

# macOS
brew services stop mongodb-community

# Linux
sudo systemctl stop mongod
```

---

## 📞 پشتیبانی

اگر با مشکلی مواجه شدید که در این راهنما حل نشده:

1. **لاگ‌های خطا را بررسی کنید:**
   - Backend logs در Terminal که Backend اجرا کردید
   - Frontend logs در Console مرورگر (F12)
   - MongoDB logs: `tail -f /var/log/mongodb/mongod.log`

2. **فایل‌های `.env` را دوباره چک کنید**

3. **Dependencies را مجدداً نصب کنید**

---

## ✨ موفق باشید!

حالا پروژه شما روی لوکال در حال اجرا است! 🎉

آدرس‌های مهم:
- 🌐 **صفحه اصلی:** http://localhost:3000
- 🔐 **پنل ادمین:** http://localhost:3000/admin/login
- 🔧 **Backend API:** http://localhost:8001
- 📚 **API Docs:** http://localhost:8001/docs (Swagger UI)
