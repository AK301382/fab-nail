# 🚀 راهنمای راه‌اندازی خودکار Backend

## تغییرات اعمال شده

پروژه به‌گونه‌ای بهینه‌سازی شده که **دیگر نیازی به اجرای دستی فایل‌های seed ندارید**!

### ✨ ویژگی‌های جدید

#### 1. **Initialization خودکار در Startup**
Backend به‌طور خودکار هنگام راه‌اندازی این کارها را انجام می‌دهد:

- ✅ **ایجاد Index های دیتابیس** برای بهبود سرعت query ها
- ✅ **بررسی و ایجاد داده‌های پیش‌فرض** (فقط در صورت خالی بودن)
- ✅ **آماده‌سازی Collections** برای استفاده فوری

#### 2. **داده‌های پیش‌فرض**
اگر دیتابیس خالی باشد، این داده‌ها به‌طور خودکار ایجاد می‌شوند:

**Service Categories (6 دسته):**
- Manicures / Maniküre / Manucures
- Pedicures / Pediküre / Pédicures
- Extensions / Verlängerungen / Extensions
- Add-ons / Zusatzleistungen / Suppléments
- Quick Services / Schnellservice / Services rapides
- Massage / Massage / Massage

**Gallery Styles (8 استایل):**
- French, Classic, Modern, Artistic
- Minimalist, Glitter, Ombre, Matte

**Gallery Colors (12 رنگ):**
- Pink, Red, Purple, Blue, Green, Yellow
- Gold, Silver, Black, White, Nude, Orange
- با کد hex برای هر رنگ

#### 3. **پشتیبانی چند زبانه**
تمام داده‌های پیش‌فرض شامل ترجمه به سه زبان:
- 🇩🇪 آلمانی (German)
- 🇬🇧 انگلیسی (English)
- 🇫🇷 فرانسوی (French)

## 📋 نحوه استفاده

### راه‌اندازی اولیه (بدون نیاز به seed)

```bash
# 1. نصب dependencies
cd /app/backend
pip install -r requirements.txt

# 2. راه‌اندازی backend (خودکار همه چیز را initialize می‌کند)
sudo supervisorctl restart backend

# 3. بررسی لاگ‌ها
tail -f /var/log/supervisor/backend.err.log
```

### لاگ‌های Startup

هنگام راه‌اندازی، این پیام‌ها را خواهید دید:

```
2025-11-09 11:54:29,890 - server - INFO - 🚀 Starting database initialization...
2025-11-09 11:54:29,890 - server - INFO - 📊 Creating database indexes...
2025-11-09 11:54:29,914 - server - INFO - ✅ Database indexes created
2025-11-09 11:54:29,916 - server - INFO - 📂 Creating default service categories...
2025-11-09 11:54:29,924 - server - INFO - ✅ Created 6 default categories
2025-11-09 11:54:29,926 - server - INFO - 🎨 Creating default gallery styles...
2025-11-09 11:54:29,928 - server - INFO - ✅ Created 8 default styles
2025-11-09 11:54:29,930 - server - INFO - 🌈 Creating default gallery colors...
2025-11-09 11:54:29,932 - server - INFO - ✅ Created 12 default colors
2025-11-09 11:54:29,932 - server - INFO - ✨ Database initialization completed successfully!
```

## 🎯 استفاده از پنل ادمین

### دسترسی به پنل
1. مرورگر را باز کنید
2. به `/admin/login` بروید
3. اطلاعات ورود پیش‌فرض:
   - **Username:** admin
   - **Password:** admin123

### امکانات پنل ادمین

بدون نیاز به seed کردن داده، می‌توانید:

✅ **Services** - سرویس‌های جدید اضافه کنید
✅ **Artists** - هنرمندان را مدیریت کنید
✅ **Gallery** - تصاویر گالری را آپلود و مدیریت کنید
✅ **Appointments** - رزروها را مشاهده و مدیریت کنید
✅ **Messages** - پیام‌های تماس را مشاهده کنید
✅ **Settings** - تنظیمات سایت را تغییر دهید

## 🔄 بازنشانی دیتابیس

اگر می‌خواهید دیتابیس را از نو بسازید:

```bash
# پاک کردن دیتابیس
mongosh --quiet --eval "use fabulous_nails; db.dropDatabase()"

# Restart backend (خودکار همه چیز را دوباره می‌سازد)
sudo supervisorctl restart backend
```

## 📊 بررسی وضعیت دیتابیس

```bash
# چک کردن تعداد داده‌ها
mongosh --quiet --eval "use fabulous_nails; 
  print('Categories:', db.service_categories.countDocuments({}));
  print('Styles:', db.gallery_styles.countDocuments({}));
  print('Colors:', db.gallery_colors.countDocuments({}));
  print('Services:', db.services.countDocuments({}));
  print('Artists:', db.artists.countDocuments({}));
  print('Gallery:', db.gallery.countDocuments({}));"
```

## ⚠️ نکات مهم

1. **فایل‌های Seed دیگر لازم نیستند**
   - `seed_categories_styles.py` ❌
   - `seed_data.py` ❌
   - `setup_indexes.py` ❌

2. **Initialization هوشمند**
   - اگر داده وجود داشته باشد، دوباره seed نمی‌شود
   - فقط Index ها در هر startup بررسی می‌شوند

3. **پنل ادمین فوراً آماده است**
   - بدون نیاز به داده‌های نمونه
   - همه قابلیت‌ها کار می‌کنند
   - می‌توانید از صفر شروع کنید

## 🎨 افزودن داده از پنل ادمین

### مثال: افزودن یک Service جدید

1. به `/admin/services` بروید
2. روی "Add New Service" کلیک کنید
3. فرم را پر کنید:
   - نام سرویس (آلمانی)
   - توضیحات (آلمانی)
   - دسته‌بندی (از لیست کشویی)
   - قیمت
   - مدت زمان
   - تصویر (اختیاری)
4. ذخیره کنید
5. **خودکار به انگلیسی و فرانسوی ترجمه می‌شود!** 🌍

### مثال: افزودن تصویر به Gallery

1. به `/admin/gallery` بروید
2. روی "Add New Image" کلیک کنید
3. تصویر را آپلود کنید
4. عنوان بدهید (آلمانی)
5. نام هنرمند را وارد کنید
6. استایل را انتخاب کنید
7. رنگ‌ها را انتخاب کنید
8. ذخیره کنید

## 🔧 عیب‌یابی

### اگر categories خالی است:

```bash
# چک کنید backend در حال اجراست
sudo supervisorctl status backend

# لاگ‌ها را بررسی کنید
tail -n 100 /var/log/supervisor/backend.err.log

# دوباره restart کنید
sudo supervisorctl restart backend
```

### اگر پنل ادمین load نمی‌شود:

```bash
# چک کنید frontend در حال اجراست
sudo supervisorctl status frontend

# restart کنید
sudo supervisorctl restart frontend
```

## 📝 تغییرات فنی

در `server.py` یک event handler جدید اضافه شده:

```python
@app.on_event("startup")
async def startup_db_init():
    # Initialization logic here
```

این handler:
- Index ها را ایجاد می‌کند
- Collections خالی را با داده‌های پیش‌فرض پر می‌کند
- Error handling کامل دارد
- Log های واضح و مفید ارائه می‌دهد

## 🎉 نتیجه

الان شما یک **Backend خالی و آماده** دارید که:
- ✅ بدون نیاز به seed دستی کار می‌کند
- ✅ پنل ادمین کاملاً فعال است
- ✅ می‌توانید از پنل تمام داده‌ها را مدیریت کنید
- ✅ Translation خودکار فعال است
- ✅ همه چیز بهینه و سریع است

**شروع کنید و لذت ببرید!** 🚀
